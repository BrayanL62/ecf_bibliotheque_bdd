{% extends 'base.html.twig' %}

{% block title %}Edit Kind{% endblock %}

{% block body %}
<style>
    h1{ text-align: center; margin: 15px auto}
    label, input, div { display: block; margin: 15px auto; text-align: center; }
    a { display: block; text-align: center;}
</style>
    <h1>Edit Kind</h1>

    {{ include('kind/_form.html.twig', {'button_label': 'Update'}) }}

    <a href="{{ path('kind_index') }}">back to list</a>

    {{ include('kind/_delete_form.html.twig') }}
{% endblock %}
