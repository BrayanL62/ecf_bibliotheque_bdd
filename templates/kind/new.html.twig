{% extends 'base.html.twig' %}

{% block title %}New Kind{% endblock %}

{% block body %}
<style>
    h1{ text-align: center; margin: 15px auto}
    label, input, div { display: block; margin: 15px auto; text-align: center; }
    a { display: block; text-align: center;}
</style>
    <h1>Create new Kind</h1>

    {{ include('kind/_form.html.twig') }}

    <a href="{{ path('kind_index') }}">back to list</a>
{% endblock %}
